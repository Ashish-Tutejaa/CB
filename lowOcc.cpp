//lowest occurence
#include<iostream>
//basically the ending condition is s>e.  Update an answer variable every time.
#include<math.h>
int search(int *arr,int mid,int start,int end,int elem)
    {
    if(arr[mid]==elem)
        {
        
        }
    if(arr[mid]>elem)
    if(arr[mid]<elem)
    }
using namespace std;
int main()
{
int arr[] = {1,2,2,2,2,3,4,4,5};
int size = sizeof(arr)/sizeof(arr[0]);
int end = size-1;
int mid = floor(end/2);
int n;cin>>n;
search(arr,mid,0,end,n);
}
